package pgdb

/*
import (
	"fmt"
	"testing"

	"github.com/jdetok/go-api-jdeko.me/getenv"
)

func TestInitDB(t *testing.T) {
	if err := getenv.LoadDotEnv(); err != nil {
		t.Error(err)
	}

	connStr, err := getenv.GetEnvStr("DB_CONN_STR")
	if err != nil {
		t.Error(err)
	}

	db, err := CreateDBConn(connStr)
	if err != nil {
		t.Error(err)
	}

	fmt.Println(db.Stats())
}
*/
